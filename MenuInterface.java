import java.util.ArrayList;

/**
* The MenuInterface interface defines a contract for retrieving a menu.
* It contains a single method, getMenu(), which is expected to return an ArrayList of MenuItem objects.
*/
public interface MenuInterface {

	ArrayList<MenuItem> getMenu();
	
}
